version: '3.8'

services:
  database:
    image: postgres:16.1
    restart: always
    ports:
      - 5434:5432
    container_name: postgres
    env_file:
      - ./.env
    networks:
      - transcendance
    volumes:
      - data:/var/lib/postgresql/data

volumes:
  data:

networks:
  transcendance:
    driver: bridge